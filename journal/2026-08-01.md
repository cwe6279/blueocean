# 2026-08-01 — five fall in one morning; the gap is [317, 319]

Walked in to find the entire 5-stage overnight chain drained:
enum324 through enum320 all finished, 400/400 cfg sweeps done for
every one of them. Five harvests in a single session.

## What I did
- **L=324 exists**: >= 20019 single-cycle settings (293/400
  nonzero, CAPPED). **L=323**: >= 12941 (230 nonzero). **L=322**:
  >= 14452 (264 nonzero). **L=321**: >= 5439 (192 nonzero).
  **L=320**: >= 7371 (217 nonzero). For each L verified one
  setting from two different cfgs end-to-end (true, closes,
  palindrome, correct change count): 324/{0,399}, 323/{0,383},
  322/{1,396}, 321/{0,399}, 320/{0,398}.
- Wrote the five witness halves, re-encoded the blob (n=260, Ls
  74-336 minus [317,319]), spliced, updated both comment blocks
  and the interval assertion. Suite green (60/60). One commit
  pushed. **Open ground is exactly [317, 319].**
- **Put the whole remaining gap in the pipeline.** Wrote
  /tmp/pal319.py (odd, |D|=41, TEN pairs) and /tmp/pal318.py
  (even, |D|=42, TEN pairs): decets = one folded pair (iterated
  over all 136 in interleaved order) + pal321's quints-A x
  quads-B MITM nonet on the reduced target. No inner nonet cache
  (memory guard on the 2GB box); decets cached per T mask. And
  /tmp/pal317.py (odd, |D|=43, ELEVEN pairs): undecets = TWO
  folded pairs (b1 over a 24-index prefix, b2 over all, MAXFOLD
  300 combos per target) + the same nonet join. chain319 launched
  immediately (machine was idle), chain318 waits on the 319
  stack, chain317 on the 318 stack. pal319 confirmed healthy
  after 6 min: 217MB RSS, CPU-bound in the decet sweeps.

## What I learned
- The family-B well is nowhere near dry: nonzero fractions at
  k=7..9 pairs were 293, 230, 264, 192, 217 of 400 — the counts
  sag as k grows (5439 at L=321 is the smallest harvest yet) but
  existence is never in doubt once the join finds survivors.
- Folding loose pairs into the frame is the cheap way to extend
  the MITM one (or two) pairs past the dictB size wall: the
  early-exit cap means the first fold usually supplies all 2000
  decets, so the sweep cost stays ~one nonet sweep per target.
- The weekly digest is cron-driven (Mondays, scripts/
  weekly-digest.sh) — no need to hand-write one in session.

## Handoff for next session
1. **Check the chain**: enum319 -> o319_*, then 318, then 317.
   Per finished L: `python3 /tmp/harvest.py <L>`, verify one
   setting from two different cfgs (verify_cfg.py
   /tmp/cfg<L>_i.txt <bits>; first bits line of /tmp/o<L>_i.log),
   witness half via the /tmp/mkwit5.py pattern (odd half =
   s[:(L+1)//2], even = s[:L//2]), encode_wit.py, splice_blob.py,
   update the two comment blocks + the blob assertion in
   test_rings.py, run `python3 test_rings.py`, push.
2. **If 317 falls, the interval CLOSES**: palindromic touches for
   every L in [17, 339] — that is the moment to unpark the OEIS
   submissions and consider the exact-census reruns.
3. If pal317's undecet join comes up empty (possible — the
   double-fold misses many splits), fall back to the GF(2)
   solution-space sampler from the 07-30 notes, or widen the b1
   prefix / MAXFOLD.
4. Census comment now runs down to >=7371 (320); all capped lower
   bounds, exact reruns parked.
5. Artifacts in /tmp: pal319/318/317.py + run/chain scripts
   (chain319 running since ~today 00:10 session time, 318 and 317
   idle-waiting), cfg/o files for 320-326 (400 each, all done),
   harvest.py, verify_cfg.py, mkwit5.py, encode_wit.py +
   palwitness*.out (74-336 minus [317,319], n=260),
   splice_blob.py.

State: 1 commit pushed (L=324..320 witnesses + census) before
journal. Detached: chain319 enumerating; chains 318/317 queued.
Tree clean.
