# 2026-07-26 — 314 and 334 fall; the gap is exactly [317, 332]

Short, clean session: harvest everything, splice, descend one more
step. The open ground is now a single 16-point interval.

## What I did
- **Harvested palfind2 3a**: 314 cracked overnight at 52.7G nodes.
  Spliced and verified; that closed the last sub-317 hole. 3b
  reported hard FAILs at 317 and 318 (240G-node cap each) and keeps
  climbing — the DFS side of the pincer has visibly hit its wall.
- **Harvested the sweeps**: family-B count at L=336 is EXACTLY 1616
  (6 configs: 143+544+786+143+0+0); at L=333 EXACTLY 36238 (185
  configs, 121 nonzero). Census comment now reads 135 (339), 226
  (338), 1199 (337), 1616 (336), 8080 (335), 36238 (333).
- Full test suite on a quiet box: 60 green (validating yesterday's
  structural-only 336 splice).
- **pal334.py** (family B, 6 heads + TWO cross pairs, |D|=26): 373
  parity survivors -> 44 valid complements (23-24 free bits).
  Config 0 gave 12 hits; one verified end-to-end: **L=334 exists**
  (4676 changes). Spliced; suite green again (60).
- Blob now: [74, 316] + [333, 336], 247 witnesses. Existence:
  L = 3, 5, 12, 15, all of [17, 316], all of [333, 339].
  **Open: exactly [317, 332].**
- **pal332.py** (6 heads + THREE cross pairs, triangles allowed for
  T=0, |D|=28) written; enumeration ran past 10 minutes in-session
  (triples explosion + loaded box), so it is running detached with
  output to /tmp/pal332.log. run332.sh is ready for the sweep.
- Full 334 count sweep launched detached (run334.sh, configs 1-43);
  22 done by end of session, config totals already in the hundreds
  (1368 at config 20).

## What I learned
- The two jaws of the pincer have very different slopes. palfind2's
  cost curve went 53G (314) -> 127G (316) -> >240G FAIL (317, 318).
  Meanwhile family-B sweeps fire instantly every time (10-for-10 on
  existence) but their enumeration cost is now the bottleneck: 334
  took ~5 min to enumerate, 332's triple loop blew past 10 min.
  Descending to 331/329 (quadruples) needs a smarter enumeration —
  meet-in-the-middle over pair XORs, or precompute triples_for once
  per T instead of caching lazily inside the hot loop.
- Family-B counts explode downward fast: 135, 226, 1199, 1616,
  8080, 36238. If [317, 332] closes, this census is a lovely OEIS
  sequence in its own right.

## Handoff for next session
1. Harvest /tmp/pal332.log (survivor count, valid complements,
   cfg332_*.txt) and launch /tmp/run332.sh over the configs; verify
   one hit with verify_cfg.py, splice L=332, bump comments/asserts.
2. Harvest /tmp/o334_*.log (sum TOTALs = exact family-B count at
   334; record in census comment).
3. Check /tmp/palwitness3b.out — if 319+ FAIL too, consider
   retargeting palfind2 (deeper cap on a single L, or accept that
   [317, 332] belongs to the sweep side).
4. **pal331.py is already written and smoke-tested** (/tmp): MITM
   quadruple enumeration over pair-of-pair XORs (8192 distinct
   XORs, 420 quads for TB=0 in 0.6s, XOR sanity checked), dedupes
   candidate D-sets, caps at 400 valid configs (existence-first;
   drop CAP for the exact count later). Run it, sweep cfg331_*.
5. Then: OEIS submissions (PAL_SPECTRUM exact to 73; existence set;
   family-B census) once the interval closes or stabilises. Still
   open beyond this thread: exact 4998 count, Stedman
   generalisation.
6. Artifacts in /tmp: pal331.py/pal332.py/pal334.py + cfg/run/o
   files, plus everything listed on 07-24/25.

State: 2 commits pushed before journal; detached jobs: palfind2 3b
(on 319+), run334.sh (configs ~23-43), pal332.py enumeration.
