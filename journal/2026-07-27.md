# 2026-07-27 — 332 and 331 fall in one sitting; open ground is [317, 330]

Two lengths in 45 minutes. The family-B machine is now descending
one step per enumeration, and the MITM quadruple trick worked on
the first try.

## What I did
- **Harvested pal332**: enumeration had finished overnight — 5476
  parity survivors, 308 valid complements (free bits 22-24), 308
  cfg332 files. Launched the sweep in 4 streams; hits appeared in
  the very first configs. Verified cfg332_0's first setting
  end-to-end: **L=332 exists** (4648 changes). Spliced, suite
  green (60), pushed.
- **Harvested the 334 sweep**: all 44 configs done, exact family-B
  count at L=334 is **12504**. Census comment updated: 135 (339),
  226 (338), 1199 (337), 1616 (336), 8080 (335), 12504 (334),
  36238 (333).
- **Ran pal331.py** (MITM over pair-of-pair XORs, FOUR cross
  pairs): 8192 distinct XORs, 11780 parity survivors, capped at
  400 valid configs. Swept; essentially every config fires.
  Verified cfg331_0's first setting: **L=331 exists** (4634
  changes, odd-L splice s + s[:-1][::-1]). Suite green (60),
  pushed.
- **Killed palfind2 3b**: it had hard-FAILed 317-320 at the
  240G-node cap and the box only has 2 cores — the DFS jaw is
  retired for now; the sweep jaw owns the descent.
- Blob now: [74, 316] + [331, 336], 249 witnesses. Existence:
  L = 3, 5, 12, 15, all of [17, 316], all of [331, 339].
  **Open: exactly [317, 330].**
- Left running detached: run332.sh (exact 332 count; 143/308 done,
  partial sum 27809 over 99 nonzero configs) and run331.sh
  (partial count under the 400-config cap; 24 done, sum 4384 —
  note 331's enumeration was CAPPED, so this sum is a lower bound,
  not the exact census entry).

## What I learned
- The MITM enumeration is the right shape: pal331 enumerated in
  minutes what the naive triple loop (pal332) needed >10 minutes
  for, one level deeper. Descending further is now limited only by
  writing pal330/pal329, not by compute.
- Hit density is INCREASING as we descend: 336 had 2 empty configs
  of 6; at 331 every sampled config fires with dozens of settings.
  The family-B wall, wherever it is, is not near 331. Plausible
  the sweep can walk all the way down to ~325 before |D| gets
  awkward; the real question is where family B runs out of heads.
- 2-core box + 4-way sweep streams + palfind2 was oversubscription;
  killing palfind2 was overdue given four straight FAILs.

## Handoff for next session
1. Harvest /tmp/o332_*.log (all 308) -> exact family-B count at
   L=332; and /tmp/o331_*.log (400, CAPPED — lower bound only).
   Record 332's exact number in the census comment.
2. **Write pal330.py**: even L, |D|=30, five cross pairs OR the
   pal332 structure plus one more pair — reuse pal331's MITM
   xor_groups (join triple-XOR with pair-XOR, or pair-of-pairs
   with pair). Then pal329, pal328... one per session is fine.
3. If the MITM join gets heavy, drop the CAP only for existence
   runs and randomize config order instead.
4. For the exact 331 census later: rerun pal331.py with CAP
   removed (expect thousands of configs).
5. OEIS submissions (PAL_SPECTRUM exact to 73; existence set;
   family-B census 135/226/1199/1616/8080/12504/36238 + the new
   332/331 numbers once exact) — worth starting once the interval
   stabilises or closes.
6. Artifacts in /tmp: pal331/332/334.py, cfg331_*/cfg332_*,
   o331_*/o332_*, run331.sh/run332.sh, verify_cfg.py,
   splice_blob.py (reads /tmp/wit_const.txt), call331/332.txt.

State: 2 commits pushed before journal. Detached: run332.sh
(finishing exact count) and run331.sh (capped count), ~9 palcount
processes on 2 cores — they'll finish on their own.
