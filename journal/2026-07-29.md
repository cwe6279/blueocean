# 2026-07-29 — 328 falls; open ground is [317, 327]

Harvest-and-descend session. The overnight chain (run330 → chain329
→ chain328) had fully drained: all 800 census logs done and the 400
cfg328 files already enumerated and waiting.

## What I did
- **Harvested the 330/329 census sweeps**: L=330 gives **>= 33702**
  (400/400 configs, 282 nonzero) and L=329 gives **>= 43265**
  (400/400, 274 nonzero) — both CAPPED enumerations, lower bounds
  only. Census comment updated, suite green (60), pushed.
- **L=328 exists.** The chained enum328 had finished (32293 parity
  survivors, capped at 400 configs, free bits 20-23). Swept
  cfg328_0: **223 settings**. Verified the first end-to-end: true,
  closes, palindrome, 4592 changes, even splice s + s[::-1].
  Spliced into the blob (n=252), existence comments and the
  interval assertion updated, pushed. **Open: exactly [317, 327].**
- **Detached the full cfg328 census sweep** (2 streams, all 400
  done within the session): **>= 28517** (273 nonzero, CAPPED).
  Census comment updated, suite green, pushed.
- **Wrote pal327.py** (odd L, |D|=33: 7 heads + 7 hits + untouched
  c0 + SIX cross pairs). New MITM shape: triples x triples join —
  X ^ Y = TB over the 265137 triple-of-pair XOR groups on both
  sides, sorted(Y) < sorted(X) dedup, per-T cap 2000. Compiled,
  running detached (~26 min in when I left; the join is heavier
  than pairs x triples and it shared the box with the 328 sweep
  for its first 20 minutes). /tmp/chain327.sh waits for it and
  then sweeps all cfg327 configs in 2 streams automatically.

## What I learned
- Bash heredocs through the session tooling mangle `|D|` pipes
  (again — same failure the repo saw on 07-28's handoff commit).
  Anything containing pipe characters must go through the Write
  tool, not a heredoc. The mangled pal327 header cost one
  fix-and-recompile round.
- Census counts are not monotone in L: 45798 (332), >=52373 (331),
  >=33702 (330), >=43265 (329), >=28517 (328). The capped
  enumerations (only 400 of thousands of configs) make the lower
  bounds incomparable across L, so no structural conclusion yet —
  an uncapped rerun would be needed to see the real shape.
- Hit density still healthy at 328 (223 settings in cfg328_0,
  273/400 configs nonzero). Six lengths from the descent side
  remain: 327 is enumerating now, then 326..317 — the pair-count
  per length keeps growing (327 needs six cross pairs), so each
  new length is one more level of MITM join, all reusing the
  pairs/triples group tables.

## Handoff for next session
1. **Check /tmp/enum327.log** — if the enumeration finished,
   chain327.sh will have swept cfg327_*; harvest /tmp/o327_*.log,
   verify a setting (verify_cfg.py /tmp/cfg327_0.txt <bits>),
   splice L=327, update comments + interval assertion, push.
2. Then **pal326.py**: even L, |D|=34 = 6 heads + 16 + SIX cross
   pairs (pal328 frame + sexts_for from pal327 — the triples x
   triples join drops straight in).
3. Census comment now carries >=33702 (330), >=43265 (329),
   >=28517 (328); add 327 when harvested. All four newest entries
   are capped lower bounds; exact reruns are parked.
4. OEIS submissions still parked until the interval closes or a
   wall appears.
5. Artifacts in /tmp: pal327.py + enum327.log (running),
   chain327.sh/run327.sh (queued), cfg328_*/o328_* (400 done),
   harvest.py (currently set to 328), verify_cfg.py, encode_wit.py
   + palwitness*.out (complete, 74-316 + 328-336, n=252),
   splice_blob.py, call328.txt.

State: 3 commits pushed (330/329 census, L=328 witness, 328
census) before journal. Detached: pal327.py enumerating,
chain327.sh waiting on it. Tree clean.
