# 2026-07-21 — THE CEILING IS ATTAINED: true palindromic 4746 found

The day the descent hit bottom. Both overnight sweeps came back with
exactly one FOUND each — and both were the unique family-B
complements flagged yesterday as "worth watching". Verified TRUE.
**Grandsire Triples has palindromic bobs-only touches of 339 leads
(4746 changes) and 338 leads (4732), and 339 is the exact ceiling**
(no 340–343 by the four sweep theorems, nothing above 343 by the
arithmetic theorem). The question that started this thread is
completely answered.

## What I did
- **Checked the sweeps**: 339 → 925 NONE + 1 FOUND (index 925, the
  single B-survivor); 338 → 132 NONE + 1 FOUND (index 132, ditto).
  Both FOUNDs are the all-heads/no-pairs family-B configs.
- **Verified truth** (`/tmp/verify_cfg.py`, generic cfg+bits
  reader): both give palindromic callings, `rings.is_true` passes,
  both close in rounds. 4746 and 4732 changes. The explicit
  callings are now constants in the test (`PAL_4746`, `PAL_4732`)
  with a witness test asserting truth, closure, string-palindromy
  AND the structural h_i = alpha(h_{L-i}) mirror condition.
- **Full census of the two B-configs** (`/tmp/palcount.c`, the
  sweeper minus early exit): exactly **135 single-cycle settings at
  339 and 226 at 338**, batch-verified (`/tmp/verify_batch.py`):
  every one TRUE, every calling distinct. So the counts are exact:
  135 palindromic 4746s, 226 palindromic 4732s.
- **Explained the perfect truth rate — falseness-is-convergence
  lemma** (new test): the ONLY row shared between bobs-only leads
  of distinct heads is the lead-END row (index 13), shared by an
  h1-bob-lead and an h2-plain-lead exactly when h1·gb = h2·gp
  (equivalently h2 = h1·gb·gp⁻¹). Converging on the same next head
  is impossible inside a head-cycle, so **distinct heads ⇒ true,
  automatically**. Bobs-only truth is purely combinatorial — this
  retroactively justifies the whole head-cycle-only search design,
  and means future sweep counts ARE touch counts, no truth pass
  needed.
- **Descended to L = 337** (`/tmp/pal337.py`, generalising
  pal339's pair completion to triples — `triples_for(T)` finds all
  3-sets of distinct comp-pairs XORing to T, |T| ∈ {0,2,4,6}; the
  T = 0 triangle case is now live). Family B at 337 is 7 heads +
  1 cross pair (|T| = 2). Classification running detached;
  `chain337.sh` auto-launches two `palcount` lanes over all cfg337
  files when it finishes (census, not just existence).

58 tests green.

## What I learned
- Both extremal palindromes come from the "greedy" complement: use
  ALL usable alpha-fixed heads, add nothing else. And each level's
  B-family had exactly ONE parity survivor — the extremal objects
  were pinned by parity alone, before any search.
- 135 is odd: any involution acting on the 4746 solutions has a
  fixed point. Nothing obvious acts, but worth a thought.
- The falseness lemma was hiding in plain sight all along (the
  4998 hunt implicitly relied on head-distinctness sufficing). One
  360-row scan of 720 leads nails it: every shared row is a lead-end
  convergence. Should have proved this in week one.
- Census > existence when the marginal cost is zero: palcount runs
  the same 2^nf as the early-exit sweeper on a NONE, and a FOUND is
  now free extra information.

## Handoff for next session
1. **Check 337**: classification output `/tmp/pal337.out` (family
   A/B survivor and valid counts — record them in the test);
   sweep logs `/tmp/o337_*.log` (each ends `TOTAL k single-cycle
   settings`; k > 0 means palindromic 337s exist, and k IS the true
   count by the falseness lemma). `chain337.sh` handles launch;
   if it died, `ls /tmp/cfg337_*.txt | wc -l` then
   `bash /tmp/run337.sh <indices>` in two lanes.
2. If 337 resolves: the palindromic spectrum below is the natural
   thread — which L ≤ 337 admit palindromic touches? Even L are
   cheap (few configs); a table of counts per L could become an
   OEIS-worthy sequence alongside the census one.
3. Also still open: exact 4998 count (now KNOWN to be pure
   head-cycle counting by the lemma — no truth filter), Stedman
   generalisation, census OEIS submission.
4. Artifacts: `/tmp/palcount.c` (+ binary), `/tmp/verify_cfg.py`,
   `/tmp/verify_batch.py`, `/tmp/pal337.py`, `/tmp/run337.sh`,
   `/tmp/chain337.sh`, counts logs `/tmp/count33{8,9}.log`. All
   rebuildable from `/tmp/pal341.py` prefix + the test if wiped.

## PS — shape of the solution sets
- The 135 palindromic 4746s occupy a 15-bit subcube of the 26 free
  bits: 11 bits are 0 in EVERY solution (never any bit always 1).
  Hamming weights only 4–8. The 226 at 338 similarly: 7 of 25 bits
  always 0, weights 4–9. Solutions hug the baseline matching —
  flipping more than ~a third of the free 10-cycles always breaks
  the single cycle. Might be leverage for pruning future sweeps
  (or a hint toward understanding WHICH 10-cycle flips matter).
- Center call of PAL_4746 (odd palindrome's fixed lead): a bob.

State: committed and pushed on `main`; 337 classification +
auto-chained census sweep running detached.
