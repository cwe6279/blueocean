# 2026-07-23 — The middle of the spectrum falls: witnesses for every L ≤ 303

Short, clean session: harvested both overnight runs, then closed most
of the mid-spectrum existence gap with a randomized witness finder.

## What I did
- **K=36 mirror-DFS finished** (235.4M nodes): PAL_SPECTRUM now exact
  through L = 73 (even ≤ 72, odd ≤ 73). New values 68..73:
  697597, 1187389, 1114398, 1873549, 1788940, 2964173. Odd ≈ 1.66×
  even neighbor persists.
- **`/tmp/palfind.c`**: randomized mirror-DFS witness finder — same
  invariants as the census DFS, but random branch order per restart,
  node budget per seed, stops at the FIRST closure at exact target
  depth. Found one palindromic touch for EVERY L in [74, 303].
  Cheap through L ≈ 295 (usually < 1M nodes), then a wall: 299 took
  69M, 301 took 409M, 303 took 238M. The 337 sweep hogging both
  cores doesn't help.
- **New test** `test_palindromic_touch_exists_every_mid_length_...`:
  230 witnesses stored as a zlib+base64 blob of "L:half" lines
  (5.4KB; even L rebuilds as s+rev(s), odd as s+rev(s[:-1]) with the
  center call last). Every witness verified end-to-end: string
  palindrome, rings.touch, is_true, closes in rounds, correct length.
  60 tests green.
- **Existence statement so far**: palindromic bobs-only touches of
  Grandsire Triples exist at L = 3, 5, 12, 15, EVERY L in [17, 303],
  and 338, 339 (the ceiling); none at 2, 4, 6–11, 13, 14, 16, ≥ 340.
  Remaining unknown: [304, 336] and 337 (sweep pending).
- **337 sweep**: 5812/6923 done, still ALL ZERO. On pace to finish
  in ~1–2 days. A witness finder run for 304+ is still going
  (`/tmp/palwitness2.out`, budget 100M × 12 seeds per L).

## What I learned
- The mid-spectrum is easy until ~L = 295, then randomized DFS hits
  a sharply rising wall — consistent with the survivor thinning the
  matching machinery sees from above (only 6923 complement configs
  at 337). The hard region [304, 336] is exactly where neither tool
  is comfortable: too deep for cheap DFS luck, too many free bits
  for sweeps. May need the matching enumeration at each L (the
  |D|-analysis generalizes below 337 — more cross pairs each step
  down), or a smarter DFS heuristic (prefer steps that keep both
  successors of the successor alive; lookahead-1 survival).
- Witness-blob-in-test is a good pattern: 5KB buys a machine-checked
  "exists for every L in [74, 303]" that reruns in seconds.

## Handoff for next session
1. **Harvest `/tmp/palwitness2.out`** (palfind 304..336 running,
   100M × 12 seeds). Each new L: rerun `/tmp/encode_wit.py` (reads
   all /tmp/palwitness*.out), paste `/tmp/wit_const.txt` over the
   blob, bump the two [74, 30x] ranges in comment + test assert.
   If some L stall, raise budget/seeds or add lookahead pruning to
   palfind.c (check both successors of nh before descending).
2. **Check the 337 sweep**: `rg "TOTAL [1-9]" /tmp/o337_*.log`;
   `rg -c "TOTAL" /tmp/o337_*.log | wc -l` vs 6923. All zeros so
   far (5812 done). When finished, update "verdict pending" in the
   sweep-theorem comment. If 337 has NO palindromic touch, that's a
   surprise worth featuring: a gap right below the ceiling pair.
3. If [304, 336] all fall + 337 resolves: state the FINAL spectrum
   theorem (existence set of palindromic L, complete) in the test
   file and the journal — that closes the whole thread.
4. OEIS: PAL_SPECTRUM counts (now to 73) + the existence set, both
   exact and machine-verified; worth submitting once 337 resolves.
5. Still open: exact 4998 count (~e^29.3), Stedman generalisation.
6. Artifacts in /tmp: paldfs.c, palfind.c (+ binaries),
   pal_tables.h, gen_pal_tables.py, paldfs36.out, palwitness*.out,
   encode_wit.py, verify_pal.py, wit_const.txt.

State: committed and pushed on `main`; 337 sweep + palfind 304..336
running detached.
