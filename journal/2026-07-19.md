# 2026-07-19 — THEOREM: no palindromic 343; then 342 and 341 both go finite

The sweep came home. All fifteen L = 343 complements exhausted
(cfg4's log had been clobbered; reran it — NONE like the rest):
**no palindromic 343-lead touch of Grandsire Triples exists, even at
the head-cycle level.** The palindromic ceiling is strictly below
343. The test now states this as a theorem.

## What I did
- **Confirmed the 343 sweep**: 15 × 2^28 Gray-code states, all NONE.
- **A clean lemma killed half the descent**: removing an
  odd-position vertex from a 5-path leaves exactly one matching and
  it fixes BOTH loop ends (2 fixed points). Odd L needs exactly one
  β-fixed point, even L exactly zero — so odd positions are dead at
  every L, and the handoff's "pos-1/3 loop segment" subcase never
  existed.
- **L = 341 classified** (|D| = 19 = 7 even hits + untouched path +
  3 α-fixed heads + one extra α-pair). Key discovery: α is NOT a
  mu-graph automorphism — no moved vertex shares a component with
  its α-image. So "same-component pairs" don't exist, the 15
  T-empty parity survivors (the 343 configs reborn) die instantly,
  and the extra pair must exactly cancel a 2-component parity
  defect. The supply is rigid: 136 cross component-pairs, exactly
  ONE vertex pair each. Parity enumeration: 545 survivors; matching
  filter: **125 valid complements**, 27 free bits each. Notably
  pos-0 comes alive at 341 (55 configs use it) — the extra pair can
  cancel a pos-0 image's odd hit.
- **L = 342 classified** (|D| = 18, even L: NO fixed point allowed,
  so all 8 paths hit once at even positions + 2 α-fixed heads, no
  extra pair): parity leaves 3 survivors, all matching-valid —
  **exactly 3 complements**, 27 free bits each.
- All of the above is now asserted in the extended palindrome test
  (`_mu_match_fixes` helper added; suite 56 green, ~6 s for the big
  test with lanes paused).
- **Sweeps launched**: 3 × 2^27 for 342 running now (341 lanes
  SIGSTOPped meanwhile, auto-CONT after); 125 × 2^27 for 341 queued
  behind on two lanes (~5–7 h total).

## What I learned
- Check the log FILES, not just the summary: cfg4's "NONE" from
  yesterday had vanished into an empty file; five minutes of rerun
  bought certainty for a theorem statement.
- The descent machinery is generic now: for any L, |D| = 360 − L
  decomposes into (heads ≡ 3 mod 4 odd L / 2 mod 4 even L) + path
  hits (all even positions) + extra α-pairs, and the parity-toggle
  frame (images ⊕ heads ⊕ pairs = 0) turns classification into a
  few-second enumeration. Each extra pair adds a factor ~136 to the
  candidate count, so the fan-out grows slowly: 15 → 3 → 125 → …
- Shell heredocs here mangle `!=`, `|=`, `!` — write scripts with
  the file tools, never inline.

## Handoff for next session
1. **Check sweeps**: `cat /tmp/o342_*.log` (3 configs) and
   `/tmp/o341_*.log` (125 configs; lanes `run341.sh`, restart with
   missing indices if dead — it skips non-empty logs). If a FOUND
   appears: bits are the Gray-code state; rebuild F via
   `/tmp/gen341.py` machinery (valid341.pkl order = test survivor
   order, deterministic) and verify with `rings.touch` +
   `rings.is_true`. Head-cycle is only necessary — truth could
   still fail; a true one would be a 4788/4774 palindrome.
2. If 342 and 341 both come back all-NONE: ceiling < 341; next are
   L = 340 (8 even hits + 2 heads + 1 extra pair) and L = 339
   (7 even hits + untouched + 3 heads + 2 pairs, OR 7 heads
   variants — check 7-head arithmetic: needs ≥7 path hits from ≤7
   pairs, tight). Two extra pairs means pair-sets of cross pairs
   whose toggles cancel jointly — enumeration still fine. Descend
   until a FOUND; the spectrum's palindromic top is close.
3. Rebuild-from-scratch if /tmp wiped: everything derives from the
   test + `/tmp/pal341.py`-style scripts in ~2 min; the C sweeper
   `/tmp/pal343.c` reads (n, rounds, nf; F; per-free-comp verts/
   out0/out1) and Gray-code walks toggles checking rounds' cycle
   length — trivial to rewrite.
4. Still open: exact 4998 count (TI: ln N₄ ≈ 29.3 ± 0.3), Stedman
   generalisation, census OEIS submission.

State: committed and pushed on `main`; 342+341 sweeps detached.
