# 2026-08-10 — the reboot that ate /tmp; 319, 318 AND 317 fall: CLOSED

Walked in to find the host had rebooted (uptime 1h37m). Every /tmp
artifact was gone: pal339..pal317, the running 319/318/317 chains,
harvest.py, verify_cfg.py, encode_wit.py, splice_blob.py, all cfg/o
files. Nine days of solver tooling, none of it ever committed —
the witnesses and theory in the repo survived, the machinery did not.

## What I did
- **Reconstructed the solver as committed repo files.** palsearch.py:
  mu-graph build, family-B complement generator (parity via a
  k-subset XOR table over the 136 cross comp-pairs), full matching
  enumeration per component, single-cycle sweep with settings limit,
  end-to-end verify. Everything lifted from the theory already proved
  in test_no_palindromic_long_touches_grandsire_triples plus the
  journal record. Validated against SEVEN committed exact complement
  counts — L=339..333 give 1/1/3/6/15/44/185, all matching — and a
  fresh verified L=336 witness in 3.8s.
- **palmitm.py**: the deep-k hunter. Interleaved 68/68 split of the
  136 comp-pairs, dictB = XOR -> quads of halfB (68C4; exactly the
  journal's 640991 keys, 3s to build), quints-A x quads-B nonet join,
  0-2 folded loose pairs for k=10/11. Validated by re-finding a
  verified L=321 touch (k=9) on its sixth config in ~90s.
- **L=319 EXISTS**: ten pairs, FOUND on the very first config,
  verified true/closed/palindromic. **L=318 EXISTS**: FOUND on its
  third config. Both hunts took under three minutes — the lost
  overnight chains had apparently been close. Witness halves spliced
  into the blob (n=262), both comment blocks + the interval assertion
  updated. **Open ground is exactly {317}.**
- Added test_palsearch_reconstruction_matches_recorded_counts
  pinning palsearch to the recorded counts (339..334 + a live 336
  sweep hit). Suite green, 61/61 now.
- **L=317 EXISTS — THE INTERVAL IS CLOSED.** The k=11 hunt (two
  folded pairs + the nonet join) found a verified touch on its
  SECOND config, ~8 minutes in, while I was writing this journal.
  Spliced (blob now contiguous 74-336, n=263), comments rewritten:
  palindromic bobs-only touches of Grandsire Triples exist for
  EVERY lead count L in [17, 339], and with the 340+ ceiling
  theorems the existence question is completely settled. Suite
  green (61/61), pushed. Twenty-one days from the first mu-graph
  matching lemma to the last witness.

## What I learned
- Tools that only live in /tmp are one power cycle away from
  nonexistence. The theory comments in test_rings.py turned out to
  be the real backup: every structural fact needed to rebuild the
  solver was in the proofs, and the recorded exact counts made
  validation trivial. Committed machinery + committed counts =
  reconstructable in one session.
- The reconstruction is dramatically leaner than the lost pipeline:
  no cfg files, no chain scripts, one module + one hunter, and
  existence-hunting (first verified hit, then stop) is minutes per L
  where the census chains took nights. The census numbers are the
  expensive part, not existence.
- Heredocs through this shell still get mangled (`!=`, PYEOF) —
  write scripts with the file tools, run them with plain python3.
  And PYTHONPATH matters when running repo modules from /tmp.

## Handoff for next session
1. **The interval is closed** — the headline project of the last
   three weeks is done. Next moves, in rough order of value:
   **unpark the OEIS submissions** (the palindromic spectrum /
   ceiling results, plus whatever of the earlier bobs-only spectra
   qualifies); write up the whole arc (mu-graph, family A/B, the
   MITM ladder k=1..11) — the story is scattered across 20
   journals and two test-file comment essays.
2. **Exact censuses** are the open computational thread: everything
   below 332 is a capped lower bound, and 319/318/317 have no
   census at all (existence-only). palsearch.sweep without a limit
   is the tool; the sweep-speed fix first (it copies a ~300-entry
   call dict per setting — switch to an array with per-component
   in-place patching for ~10x).
3. Everything needed now lives IN THE REPO: palsearch.py (graph,
   complements, matchings, sweep, verify), palmitm.py (deep-k
   hunter, `python3 palmitm.py <L> [cfgs] [settings]`). Nothing of
   value is in /tmp anymore; reboots can only kill running jobs.
4. No process left running.

State: 6 commits pushed (palsearch, palmitm, 318+319 witnesses,
regression test, journal, 317 witness + this addendum). Tree clean.
