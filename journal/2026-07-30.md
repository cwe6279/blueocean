# 2026-07-30 — 327 falls; open ground is [317, 326]; 5-deep chain

Harvest-and-pipeline session. The overnight chain327 had fully
drained: enum327 done (70317 parity survivors, capped 400 configs,
free bits 20-22) and all 400 cfg327 sweeps finished and waiting.

## What I did
- **L=327 exists.** Harvested /tmp/o327_*.log: **>= 31544**
  single-cycle settings (400/400 done, 291 nonzero, CAPPED).
  Verified two settings end-to-end from different configs
  (cfg327_0 and cfg327_396): true, closes, palindrome, 4578
  changes, odd splice s + s[:-1][::-1]. Spliced into the blob
  (n=253, Ls 74-336 minus the gap), updated existence + census
  comments and the interval assertion, suite green (60), pushed.
  **Open: exactly [317, 326].**
- **Built a 5-deep overnight pipeline** in /tmp, each stage
  waiting on the previous via pgrep loops:
  1. **pal326.py** (even L, 6 heads + SIX cross pairs, triples x
     triples MITM — pal327's join on pal328's frame) launched now,
     chain326.sh sweeps its cfgs when the enum ends.
  2. **pal325.py** (odd L, SEVEN pairs: triples x (triples + one
     single) join, XT hoisted out of the inner loop) via
     chain325.sh.
  3. **pal324.py** (even L, SEVEN pairs, same join) via
     chain324.sh.
  4. **pal323.py** (odd L, EIGHT pairs) via chain323.sh — NEW
     MITM shape: int-bitmask encoding of comp sets, interleaved
     half-split of the 136 pairs, quads-of-half-A x
     quads-of-half-B (68C4 = 814385 keys fits RAM where the
     frozenset xor4 table would not; misses 5+3 splits, fine for
     capped existence mining).
  5. **pal322.py** (even L, EIGHT pairs — pal323's bitmask
     half-split on the even frame) via chain322.sh.
- Parameterized /tmp/harvest.py (takes L on argv instead of an
  edit per length).

## What I learned
- The ascent side is genuinely walled: journal 07-26 records the
  DFS cost curve 53G nodes (314) -> 127G (316) -> >240G FAIL
  (317/318). So the descent (family B + MITM) is the only live
  route into [317, 326].
- The k-pair MITM ladder so far: 3 pairs exact (332), 4 pairs
  pairs x pairs (331/330), 5 pairs x triples (329/328), 6 triples
  x triples (327/326), 7 triples x (triples+1) (325/324),
  8 half-split quads x quads with bitmasks (323). Deeper (k=9..11
  for 322..317) needs the bitmask trick everywhere; on this 2GB
  box the half-split with capped value lists is the only shape
  that fits. A GF(2) linear-algebra sampler (solution space of
  the subset-XOR system, filtered by weight) is the fallback if
  half-split misses too much.
- python3 with stdout redirected block-buffers everything: the
  enum logs stay empty for the whole run and flush at exit. Use
  cfg-file counts to gauge progress, or add -u next time.

## Handoff for next session
1. **Check the chain**: /tmp/enum326.log then o326_*.log, then
   325, 324, 323, 322 in order. For each finished length: harvest
   (`python3 /tmp/harvest.py <L>`), verify a setting
   (verify_cfg.py /tmp/cfg<L>_0.txt <bits from head -1 of a
   nonzero o<L>_*.log>), add half to /tmp/palwitness<L>.out
   (odd half = s[:(L+1)//2], even = s[:L//2]), encode_wit.py +
   splice_blob.py, update existence + census comments + the
   blob assertion ranges, run tests, push.
2. If the whole chain drained, next is **pal321.py** (odd L,
   NINE pairs: quads x (quads+1) on the bitmask half-split, or a
   68/68 quads x quints split with the bigger dictB).
3. Census comment now ends ... >=28517 (328), >=31544 (327); all
   five newest are capped lower bounds, exact reruns parked.
4. OEIS submissions still parked until the interval closes or a
   wall appears.
5. Artifacts in /tmp: pal326/325/324/323/322.py + run/chain
   scripts (chains 325/324/323/322 idle-waiting, enum326
   running), cfg327_*
   + o327_* (400 done), harvest.py (argv), verify_cfg.py,
   encode_wit.py + palwitness*.out (74-316 + 327-336, n=253),
   splice_blob.py.

State: 1 commit pushed (L=327 witness + census) before journal.
Detached: pal326 enumerating; chain326/325/324/323/322 queued
in sequence. Tree clean.
