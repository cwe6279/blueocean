# 2026-07-24 — 337 falls to family B (exactly 1199), and the gap starts to crumble

Big day. The 337 sweep finished overnight and the verdict is the
opposite of the "all zeros" trend: palindromic 337s EXIST, and the
way they exist confirms the deepest pattern of this thread.

## What I did
- **337 sweep verdict**: all 6923 configs done. EXACTLY 1199
  single-cycle settings — and every single one lives in the three
  family-B complements (indices 6920/6921/6922: 202 + 275 + 722);
  all 6920 family-A complements came back empty. Batch-verified all
  1199: true, distinct, palindromic, 4718 changes. So Grandsire
  Triples has exactly 1199 palindromic 4718s, and family B (all 7
  usable alpha-fixed heads + minimal cross pairs) has now produced
  EVERY palindromic touch at L = 337, 338, 339 while family A has
  produced none. Added `PAL_4718` witness + the sweep-verdict
  theorem comment; the ceiling test now checks 339/338/337.
- **Family-B pincer** (`/tmp/pal335.py`): since only family B ever
  fires near the top, enumerate ONLY family B at L = 335 (7 heads +
  TWO cross pairs jointly cancelling TB). Enumeration took ~30s: 129
  parity survivors -> 15 valid complements, 24 free bits each.
  Sweep (15 x 2^24, `/tmp/run335.sh`) is mostly done and struck
  immediately: 6000+ single-cycle settings so far across 11/15
  configs, every config nonzero so far. One witness verified and in
  the blob: **L = 335 exists** (4690 changes).
- **`/tmp/palfind2.c`** — lookahead-1 survival ordering for the
  randomized witness DFS: children whose own successors are both
  unvisited are tried first (random tie-break), dead children
  (no live successor before final depth) are skipped. Night-and-day:
  L = 305 fell in 607M nodes and 306 in 277M where plain random
  order had failed at 14.4G each. Both harvested and verified.
  Detached runs on [305,314] (`/tmp/palwitness3a.out`) and
  [316,336] (`/tmp/palwitness3b.out`), budget 10G x 24 seeds.
- **Harvested yesterday's palwitness2**: 304 (1.18G nodes) and 315.
- **Existence now**: L = 3, 5, 12, 15, EVERY L in [17, 306], 315,
  335, 337, 338, 339. Open: [307, 314], [316, 334], 336. All
  witnesses machine-checked in the blob test; 60 tests green.

## What I learned
- Family B is not just where the extremal palindromes live — it is
  (so far) the ONLY place palindromes live at the top of the
  spectrum. Three consecutive full censuses (339: 135, 338: 226,
  337: 1199) with family A contributing zero every time. If that
  persists, the family-B-only enumeration IS the tool for the whole
  hard region: 335 cost seconds to enumerate and minutes to sweep.
- Counts are climbing steeply as L descends: 135 -> 226 -> 1199 ->
  ~6000+ (335, family B alone, still counting). The "solutions hug
  the baseline matching" picture from 07-21 is loosening fast.
- One cheap heuristic (survival-ordered branching) was worth ~50x
  on the DFS wall. The failure mode of plain random order near the
  wall was evidently walking into successor-starved corridors.

## Handoff for next session
1. **Harvest the three detached runs**:
   - `/tmp/o335_*.log` (15 configs, likely finished minutes after
     session end): sum TOTALs = exact family-B count at 335; record
     in the test comment next to the 337 verdict.
   - `/tmp/palwitness3a.out` / `3b.out` (palfind2 on [305,314] /
     [316,336]): each FOUND -> rerun `/tmp/encode_wit.py`, paste
     `/tmp/wit_const.txt` over the blob (`/tmp/splice_blob.py` does
     this), bump the ranges in the two comments + the assert.
2. **Descend family B further** (the likely closer): copy
   `/tmp/pal335.py` -> pal333.py with THREE cross pairs cancelling
   TB (reuse triples_for from pal337.py); L=333 |D|=27. Even L
   (336, 334) need the even-case machinery from `/tmp/pal338.py`
   (6 heads, no untouched path) plus extra pairs — check pal338.py
   for the |D| arithmetic. If family B keeps firing, the whole gap
   [307,336] could close from above while palfind2 closes from
   below; then state the FINAL existence theorem: palindromic L =
   {3, 5, 12, 15} u [17, 339] minus whatever holds out.
3. OEIS: PAL_SPECTRUM (exact to 73) + existence set + the top-end
   census counts (135/226/1199/...) once the spectrum is complete.
4. Still open: exact 4998 count, Stedman generalisation.
5. Artifacts in /tmp: palfind2.c (+ binary, the good one),
   pal335.py, run335.sh, splice_blob.py, encode_wit.py,
   verify_cfg.py, verify_batch.py, o335_*.log, o337_*.log,
   palwitness*.out, wit_const.txt.

State: committed and pushed on `main` (3 commits); palfind2 x2 and
the 335 sweep tail running detached.
