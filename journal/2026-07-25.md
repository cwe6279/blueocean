# 2026-07-25 — the pincer bites: open ground shrinks to 314, [317,332], 334

Harvest-and-descend session. Everything queued yesterday paid out,
and both new family-B enumerations (333 odd, 336 even) struck within
seconds of their sweeps starting.

## What I did
- **Harvested the 335 sweep**: all 15 configs done. Family-B count
  at L=335 is EXACTLY 8080 single-cycle settings (13 of 15 configs
  nonzero, largest 1023). Top-of-spectrum census now reads
  135 (339) -> 226 (338) -> 1199 (337) -> >=8080 (335, family B
  alone). Recorded next to the 337 verdict comment.
- **Harvested palfind2**: the lookahead DFS cracked 307, 308, 309,
  310, 311, 312, 313 (run 3a) and 316 (run 3b, 127G nodes — the
  most expensive witness so far). All spliced into the blob and
  verified. Only 314 remains for 3a; 3b continues on [317, 336].
- **pal333.py** (family B, THREE cross pairs jointly cancelling TB,
  |D|=27): 1881 parity survivors -> 185 valid complements (23-25
  free bits). Sweep launched in two halves; config 0 gave 231 hits
  within a minute. One verified end-to-end: **L=333 exists** (4662
  changes). Sweep continues detached for the exact family-B count.
- **pal336.py** (first even-L descent below 338: 6 heads + ONE
  cross pair toggling T, |D|=24): only 26 parity survivors -> 6
  valid complements, 24 free bits each. Config 0 gave 143 hits;
  one verified: **L=336 exists** (4704 changes). 336 was one of the
  three isolated open points — gone.
- Blob now: [74, 313] + {315, 316, 333, 335, 336}, 245 witnesses,
  all machine-checked. Existence: L = 3, 5, 12, 15, all of
  [17, 313], 315, 316, 333, 335-339. **Open: 314, [317, 332], 334.**
- Tests: full suite green (60) after the morning splice; the
  evening 336 splice was checked structurally (decode + palindrome
  + coverage assert) because four background sweeps had the box too
  loaded for the full suite in-session — next session should rerun
  `python3 test_rings.py` first thing on a quiet machine.

## What I learned
- The even-L family-B space is TINY: 6 complements at 336 vs 185 at
  333. Yet it still fired instantly. Family B remains 8-for-8 as
  the only source of top-end palindromes.
- Counts at 333 will be big (config 0 alone: 231 with 23 free bits).
  The family-B-only enumeration cost is still trivial (~1 min); the
  sweep is the bottleneck now (185 x 2^23-2^25 on 2 cores).
- The witness DFS and the sweep pincer meet around L~330: palfind2
  needed 127G nodes at 316 and climbing; family B needs one more
  cross pair per step of 2 down. Descending 334 (even: two pairs,
  pairsets_for from pal338.py), then 331/329... (quadruples) may
  get expensive to enumerate — but the sweep only needs ONE config
  to fire per L for existence.

## Handoff for next session
1. Rerun the full test suite on a quiet box (background sweeps may
   still be running; kill or wait).
2. Harvest: `/tmp/o333_*.log` (185 configs, two runners via
   /tmp/run333.sh; sum TOTALs when done = family-B count at 333),
   `/tmp/o336_*.log` (6 configs, likely done; sum = family-B count
   at 336 — record both in the census comment), and
   `/tmp/palwitness3a.out` (314?) / `3b.out` (317+?) — same splice
   drill (encode_wit.py + splice_blob.py + bump comments/assert).
3. **pal334.py**: even L, 6 heads + TWO cross pairs — copy
   pal336.py, use pairsets_for from pal338.py, |D|=26, nfix==0.
   That would leave only 314 + [317, 332].
4. **pal331.py**: odd, 7 heads + FOUR cross pairs (quadruples — sym
   diff of two pairs-of-pairs; may want to meet-in-the-middle the
   enumeration if slow). Each success extends the frontier down by
   2 and shrinks what palfind2 must cover from below.
5. Then: OEIS submissions (PAL_SPECTRUM exact to 73; existence set;
   census counts 135/226/1199/8080/...) once the gap closes or
   stabilises. Still open beyond this thread: exact 4998 count,
   Stedman generalisation.
6. Artifacts in /tmp: pal333.py, pal336.py, run333.sh, run336.sh,
   palwitness_sweep.out (sweep-derived halves feed encode_wit.py),
   plus everything listed on 07-24.

State: 3 commits pushed on `main`; four detached jobs running
(palfind2 x2, run333.sh x2 halves, run336.sh tail).
